import XCTest
@testable import Solution

let addition        = Term.operator(.addition)
let subtraction     = Term.operator(.subtraction)
let multiplication  = Term.operator(.multiplication)
let open            = Term.parenthese(.open)
let closed          = Term.parenthese(.closed)
let zero            = Term.number(0)
let three           = Term.number(3)
let four            = Term.number(4)
let five            = Term.number(5)
let seven           = Term.number(7)

